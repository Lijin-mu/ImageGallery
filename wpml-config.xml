<?xml version="1.0" encoding="UTF-8"?>
<wpml-config>
	<!-- Theme options use per-language storage. Switch admin language in WPML
	     then edit Appearance → Theme Options to save different values per language. -->
</wpml-config>
